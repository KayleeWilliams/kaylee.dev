---
type Props = {
  class?: string;
};

const { class: className = "" } = Astro.props;
---

<div
  class={`rounded-lg border border-border bg-card text-card-foreground shadow-xs ${className}`}
>
  <slot />
</div>
