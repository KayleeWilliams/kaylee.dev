---
type Props = {
  class?: string;
  as?: keyof HTMLElementTagNameMap;
};

const { class: className = "", as: Tag = "div" } = Astro.props;
---

<Tag class={`font-semibold text-2xl leading-none tracking-tight ${className}`}>
  <slot />
</Tag>
