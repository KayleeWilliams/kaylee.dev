{
  "$schema": "./node_modules/@biomejs/biome/configuration_schema.json",
  "extends": ["ultracite/biome/core"],
  "files": {
    "includes": [
      "**/*.{js,jsx,ts,tsx,json,jsonc,css}",
      // bench/ generated output (report HTML + result snapshots), not source.
      "!bench/benchmark-report.html",
      "!bench/results",
      "!src/**/*.astro",
      // Biome lints a .svelte file's <script> but not its {template}, so every
      // handler bound only in markup reads as "unused". Svelte has its own tooling.
      "!**/*.svelte"
    ]
  },
  "linter": {
    "rules": {
      "suspicious": {
        "noUnknownAtRules": {
          "level": "off"
        }
      }
    }
  }
}
